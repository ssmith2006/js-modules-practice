"use strict";

export function greet(name){
    return (name)
}

export function yell(str) {
  return str.toUpperCase() + "!!"; //string utility function transforms a string
}

export function whisper(str ="") {
    return str.toLowerCase() + "..."
}